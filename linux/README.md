# Telegram PC Status Bot (Linux backend)

Linux-порт сохраняет весь Telegram-интерфейс Windows-версии: inline-кнопки, вкладки аналитики, «🖥️ Железо», live-update только для активных зрителей и режимы просмотра.

Отличие только в сборе системных данных: используется psutil и best-effort инструменты X11 (xdotool/xprop/xprintidle/wmctrl) при наличии. Если утилиты недоступны, бот возвращает "Unknown", не ломая интерфейс.

## Установка

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Настройка

Создайте `.env` рядом с `main.py` в папке `linux/`:

```env
BOT_TOKEN=123456:ABCDEF
# DEFAULT_CHAT_ID по желанию
```

## Запуск

Запускайте из корня репозитория:

```bash
python ../main.py
```

(корневой `main.py` сам выберет Linux-бэкенд).

## Особенности Linux-реализации

- Аптайм, процессы, присутствие (AFK/active) и активное окно — через psutil + xdotool/xprop/xprintidle/wmctrl, где доступно.
- Железо кэшируется при старте (CPU из `/proc/cpuinfo`, GPU через `lspci`, RAM через psutil, версия ОС из `/etc/os-release`).
- State-machine, кнопки, лимиты Telegram, аналитика и вкладки идентичны Windows-версии.

## License

MIT, с атрибуцией vlalikoffc.
