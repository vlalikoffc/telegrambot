# Быстрый старт

Ниже — минимальная рабочая схема, которую можно копировать без изменений.

## 1) Создайте файл плагина

Создайте файл `plugins/hello_plugin.py`:

```python
from system.plugins import PluginBase


class HelloPlugin(PluginBase):
    name = "hello"
    version = "1.0.0"
    description = "Показывает приветствие в статусе"

    def on_render(self, render_ctx, ctx) -> None:
        # Через ctx.status можно полностью управлять статусом.
        ctx.status.add_line("")
        ctx.status.add_line("👋 Привет от плагина Hello!")
```

## 2) Создайте папку данных плагина

Плагины могут хранить файлы **только** в своей папке:

```
plugins/
  hello_plugin.py
  hello/
```

Если папка отсутствует, система создаст её автоматически при запуске.

## 3) Запустите бота

Запускайте так же, как и раньше:

```bash
python main.py
```

## 4) Проверьте, что строка появилась

Откройте статус в чате — вы должны увидеть строку `👋 Привет от плагина Hello!`.

---

Если строка не появилась:
- проверьте `windows/bot.log`;
- убедитесь, что имя файла заканчивается на `.py` и лежит в папке `plugins/`;
- проверьте, что `name` у плагина уникальный.
