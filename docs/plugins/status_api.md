# Status API

`ctx.status` — основной способ управлять текстом статуса.

## Методы

### clear()
Очищает весь текущий текст статуса.

```python
ctx.status.clear()
```

### add_line(text)
Добавляет одну строку.

```python
ctx.status.add_line("🟢 Всё хорошо")
```

### append(text)
Синоним `add_line`.

```python
ctx.status.append("➕ ещё строка")
```

### replace_section(title, lines)
Заменяет блок строк после заголовка `title`.  
Если такого заголовка нет — добавляет секцию в конец.

```python
ctx.status.replace_section("Моя секция", ["• пункт 1", "• пункт 2"])
```

## Режимы (mode)

`ctx.status.mode` показывает контекст рендера.  
Сейчас используется режим `status`. Остальные зарезервированы для будущего:

- `status`
- `hardware`
- `stats`
- `more_info`

## Примеры

### Полная замена статуса

```python
def on_render(self, render_ctx, ctx):
    ctx.status.clear()
    ctx.status.add_line("📌 Мой полностью кастомный статус")
    ctx.status.add_line("⏱️ Время: ...")
```

### Условная строка

```python
def on_render(self, render_ctx, ctx):
    if ctx.safe_state.get("viewer_count", 0) > 0:
        ctx.status.add_line("👀 Кто-то смотрит статус")
```

### Добавить секцию только при активности

```python
def on_render(self, render_ctx, ctx):
    lines = ["• пункт A", "• пункт B"]
    ctx.status.replace_section("Секция плагина", lines)
```
