# Telegram PC Status Bot

Бот обновляет **одно сообщение** (без спама) каждую секунду и показывает активное приложение, аптайм ПК и аптайм процесса активного приложения.

## Архитектура (коротко)
- `main.py`:
  - Получает foreground window через `win32gui`/`win32process`.
  - Сопоставляет процесс с `app_key`, `display_name`, припиской.
  - Каждые 1с обновляет **одно** сообщение в чатах, где включён live-режим.
  - Хранит состояние чатов в `state.json`.

## Установка

```bash
python -m venv .venv
. .venv/Scripts/activate
pip install -r requirements.txt
```

## Настройка

Создайте `.env` рядом с `main.py`:

```env
BOT_TOKEN=123456:ABCDEF
# Опционально: при запуске сразу начнёт обновлять статус в этом чате
DEFAULT_CHAT_ID=-1001234567890
```

**Важно для каналов/групп:**
- В канале бот должен быть админом и иметь право **редактировать сообщения**.
- В группе у бота должны быть права **редактировать сообщения**.

## Как узнать chat_id
- Напишите боту в личку `/start`, затем в логах (или через @userinfobot) узнайте chat_id.
- Для каналов используйте `@userinfobot` или API/логгер.

## Запуск

```bash
python main.py
```

## Команды

- `/start` — включает/продолжает live-обновление статуса в этом чате.
- `/status` — отправляет статус **один раз** (без постоянного обновления).
- Любой текст — как `/start`.

## Пример статуса

```
🖥️ Аптайм ПК: 03:12:41
🪟 Активное приложение: VS Code
💬 Приписка: страдаю хернёй (программирую)
⏱️ Аптайм приложения: 00:45:12
🔢 Процессов: 212

вот чё я делаю, но не следите пж за мной 24/7(мой юз в тг @vlalikoffc)
```

## Кастомизация

- Приписка и footer настраиваются в `main.py`:
  - `TAGLINES`
  - `FOOTER_TEXT`
- Маппинг процессов — `PROCESS_ALIASES` и `DISPLAY_NAMES`.

## Ограничения
- Windows only (используются `pywin32`).
- Заголовки окон не читаются и не логируются — только имя приложения.
