# Telegram PC Status Bot

Бот показывает статус ПК и активного приложения **только по запросу пользователя через кнопку** и обновляет одно сообщение, пока пользователь смотрит статус (до 5 минут).

## Архитектура (коротко)
- `main.py` — запускает polling, настраивает логирование и регистрирует хэндлеры/клавиатуру.
- `config.py` — базовые константы (OWNER_IDS, GitHub URL, стартовое время бота).
- `handlers.py` — обрабатывает `/start`, текст, кнопки **“👁 Показать статус”**, **“ℹ️ Больше информации”** (только для владельца), переход в табы аналитики и мгновенный просмотр кэшированного «🖥️ Железо».
- `live_update.py` — раз в секунду обновляет сообщение **только для активных зрителей**; при истечении 5 минут прячет статус и не трогает вкладки аналитики/железа.
- `messages.py` — отправка/редактирование/пин с фиксированными rate-limit (send ≥2с, edit ≥5с) и безопасным сбросом закрепа; формирует inline-клавиатуру с кнопками статуса, GitHub, аналитики и железа.
- `status.py` — сбор статуса, избранные программы, приписки, браузеры показываются как "Браузер", индикатор работы (Python/JavaScript), присутствие/AFK.
- `presence.py` — расчёт присутствия/AFK по последнему вводу и длительности текущего состояния.
- `runtime.py` — аптайм с момента старта бота.
- `windows.py` — доступ к активному окну, процессам и времени последнего ввода (Windows + `pywin32`).
- `state.py` — `state.json` с чатами, таймерами зрителей, вкладкой состояния и дневной статистикой просмотров.

## Установка

```bash
python -m venv .venv
. .venv/Scripts/activate
pip install -r requirements.txt
```

## Настройка

Создайте `.env` рядом с `main.py`:

```env
BOT_TOKEN=123456:ABCDEF
# Опционально: при запуске сразу начнёт обновлять статус в этом чате
DEFAULT_CHAT_ID=-1001234567890
```

**Важно для каналов/групп:**
- В канале бот должен быть админом и иметь право **редактировать сообщения**.
- В группе у бота должны быть права **редактировать сообщения**.

## Как узнать chat_id
- Напишите боту в личку `/start`, затем в логах (или через @userinfobot) узнайте chat_id.
- Для каналов используйте `@userinfobot` или API/логгер.

## Запуск

```bash
python main.py
```

## Команды

- `/start` — снимает закрепы, отправляет сервисное сообщение о перезапуске в старых чатах, затем показывает скрытый статус с кнопкой и закрепляет его.
- Любой текст — показывает скрытый статус с кнопкой (как `/start`).
- Кнопка «👁 Показать статус» — включает показ статуса на 5 минут для конкретного пользователя; повторные нажатия во время показа игнорируются.
- Кнопка «💻 GitHub проекта» — открывает репозиторий.
- Кнопка «ℹ️ Больше информации» — отображается только владельцу (ID из `config.py`) и переключает сообщение в режим аналитики («кто смотрел» → «статистика» → назад), без новых сообщений и с автоудалением всплывающих деталей.
- Кнопка «🖥️ Железо» — доступна при просмотре статуса, мгновенно показывает кэшированное железо (CPU/GPU/RAM/Windows/архитектура) с кнопкой «⬅️ Назад».

## Пример статуса

```
🖥️ Аптайм ПК (с запуска бота): 03:12:41
⌚ Время на моём ПК: 17:10:24
🪟 Активное приложение: VS Code
💬 Приписка: страдаю хернёй (программирую)
⏱️ Аптайм приложения: 00:45:12
🔢 Процессов: 212
🟢 За компьютером: я здесь (последний ввод 2 мин назад)


Избранные программы
▶️ Minecraft 1.21.4
🟢 Telegram
🟢 Discord
💤 Spotify
💤 OBS

🧑‍💻 Сейчас работаю:
• Python
• JavaScript

вот чё я делаю, но не следите пж за мной 24/7(мой юз в тг @vlalikoffc)
👀 Сейчас наблюдают за статусом: 3
```

## Кастомизация

- Приписка и footer настраиваются в `status.py`:
  - `TAGLINES`
  - `FOOTER_TEXT`
- Маппинг процессов и избранные приложения — `PROCESS_ALIASES`, `DISPLAY_NAMES`, `FAVORITE_APPS` в `status.py`.

## Ограничения
- Windows only (используются `pywin32`).
- Заголовки окон не читаются и не логируются — только имя приложения.

## License

This project is released under the [MIT License](LICENSE). Please retain attribution to the original author (vlalikoffc) when redistributing or modifying the code.
